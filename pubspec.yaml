name: custom_sliver_app_bar
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=2.18.6 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.5
  flutter_hooks: ^0.18.6

dev_dependencies:
  dart_code_metrics: ^5.7.0
  flutter_test:
    sdk: flutter
  lint: ^2.0.1


flutter:

  uses-material-design: true

